# Placeholder for the Authelia session cookie secret file (Docker secret).
#
# The real secrets/authelia-session.secret is git-ignored and generated by
# scripts/bootstrap-secrets.sh with: openssl rand -hex 64
# compose/ai-node.yml mounts it at /run/secrets/authelia_session_secret and
# points AUTHELIA_SESSION_SECRET_FILE at it.
replace-with-openssl-rand-hex-64-output
