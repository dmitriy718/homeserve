# Speedtest Tracker application key. Laravel format: the literal prefix
# "base64:" followed by 32 random bytes, base64-encoded.
# Generate with: printf 'base64:%s\n' "$(openssl rand -base64 32)"
APP_KEY=base64:replace-with-openssl-rand-base64-32-output
