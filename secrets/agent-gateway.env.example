# Agent gateway API key. Required; must be at least 32 characters
# (enforced by services/agent-gateway/app.py and validate-config.sh --strict).
# Generate with: openssl rand -hex 32
AGENT_GATEWAY_KEY=replace-with-openssl-rand-hex-32-output
