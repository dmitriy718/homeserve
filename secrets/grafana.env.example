# Grafana initial admin credentials (used on first start; change afterwards
# from the Grafana UI if desired). These are the only way in: anonymous
# access is disabled.
# Generate the password with: openssl rand -base64 24
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=replace-with-openssl-rand-base64-24-output
